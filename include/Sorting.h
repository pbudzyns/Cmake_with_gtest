#pragma once

#include <vector>

/**
 * @brief Sort class used for smart things.
 */
class Sort {
 public:
  /**
   * @brief Get the minimum between two values.
   * @return The smaller value.
   */
  static int Min(int v1, int v2);
};

/**
 * @brief Dummy function.
 * @param a The int value of something.
 * @param b The double value of something.
 * @param c The vector of integer values of something.
 * @return Nothing.
 */
void dummyFunction(int a, double b, const std::vector<int>& c);
